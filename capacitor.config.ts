import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'votacion_x_org',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
